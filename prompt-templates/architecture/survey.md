# 架构阶段 2：SURVEY（概览）

映射焦点区域的高层结构。

不要读文件内部，只关注结构。

记录：

- 顶层目录及其作用
- 模块/包及边界
- 入口点（main、CLI、服务端点等）
- 配置文件
- 测试结构
- 构建/包文件
- 可绘制的整体结构图素材（组件及其关系）
- 最值得深入解读的 3-5 个核心组件

大型项目使用子代理，每个模块一个。

更新 `.claude/state/session.md`：
- `current_phase`: SURVEY
- `context.high_level_map`
