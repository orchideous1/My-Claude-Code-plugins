# 架构阶段 3：DRILL（深入）

挑选最重要的组件深入理解。

对每个组件回答：

- 单一职责：
- 公开接口：
- 关键内部函数/类：
- 依赖（使用什么）：
- 被依赖（被什么使用）：
- 生命周期（何时创建、销毁）：
- 读取的配置：

每个组件使用一个 `Explore` 子代理。

更新 `.claude/state/session.md`：
- `current_phase`: DRILL
- `context.components`
