# 架构阶段 3：DRILL（深入）

挑选最重要的组件深入理解。

对每个组件回答：

- 单一职责：
- 公开接口：
- 关键内部函数/类：
- 核心代码片段（保留关键逻辑，可折叠）：
- “这段代码在做什么”的 1-3 行点评：
- 依赖（使用什么）：
- 被依赖（被什么使用）：
- 生命周期（何时创建、销毁）：
- 读取的配置：
- 设计亮点或潜在坑点（作者点评）：

每个组件使用一个 `Explore` 子代理。子代理只报告事实，不生成主观评价；主观评价由主代理在 MODEL 阶段统一撰写。

更新 `.claude/state/session.md`：
- `current_phase`: DRILL
- `context.components`
