# 架构阶段 5：MODEL（建模）

将理解综合到 `.claude/state/architecture.md`。

必须包含的章节：

1. **概述** — 一段摘要
2. **组件图** — Mermaid 或文本
3. **数据流** — 分步
4. **接口契约** — 关键 API 及其保证
5. **关键决策** — 重要架构选择
6. **开放问题** — 仍不清楚的地方

写完后检查缺口：

- 如有缺口 → 回到 DRILL
- 如关系不清 → 回到 CONNECT
- 如稳固 → 标记 DONE

更新 `.claude/state/session.md`：
- `current_phase`: MODEL
- `context.open_questions`
